

sudo kubeadm init --ignore-preflight-errors=NumCPU

mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
kubectl apply -f https://raw.githubusercontent.com/coreos/flannel/2140ac876ef134e0ed5af15c65e414cf26827915/Documentation/kube-flannel.yml

Then you can join any number of worker nodes by running the following on each as root:

kubeadm join 172.31.88.135:6443 --token l8h2pv.bsocr42l36ey7cz9 \
    --discovery-token-ca-cert-hash sha256:766de826f0efb0cc7f3e79f6d9b06e196410ac1f2a5fa1223cff1a6e00efd2dc 


---
- name: Install required system packages
  yum: name={{ item }} state=latest
  with_items:
   - 'yum-utils'
   - 'device-mapper-persistent-data'ignore-preflight-errors=NumCPU
   - 'lvm2'
   - 'ca-certificates'
   - 'curl'

- name: Add Docker GPG key.
  rpm_key:
    key: "{{ Redhat_Docker_GPG_key }}"
    state: present
    
- name: Add Docker repo
  get_url:
    url: "{{ Redhat_Docker_repo }}"
    dest: /etc/yum.repos.d/docker-ce.repo
  become: yes 

- name: install the containerd.io rpm from a remote repo
  yum:
    name: "{{ Redhat_containerdio_url }}"
    state: present

- name: Update yum and install docker-ce
  yum: name=docker-ce state=latest
  notify:
    - restart docker

- name: add ec2-user to docker group
    user:
      name: {{ Ubuntu_docker_user }}
      groups: docker
      append: yes